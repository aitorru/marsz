<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>{{title}}</title>
  <link rel="stylesheet" href="/styles.css">
  <script src="https://unpkg.com/htmx.org@1.9.5"></script>
  <script src="https://unpkg.com/hyperscript.org@0.9.11"></script>
</head>
<body class="bg-stone-800 h-screen">
  <div class="flex h-10 justify-end items-center gap-10 mx-9 my-2">
    {% for top in tops %}
      <button hx-post="{{ top.url }}" hx-trigger="click, load, every 2s" hx-target="#list-contents" class="text-stone-900 bg-stone-100 shadow hover:scale-105 shadow-white/90 rounded-md px-4 py-2">{{ top.name | safe }}</a>
    {% endfor %}
  </div>
  <div id="list-contents" class="container mx-auto flex justify-center flex-col gap-5 items-center mt-10">
    {{ content | safe }}
  </div>
  <div class="container mx-auto flex justify-center flex-col gap-5 items-center my-10 bg-stone-700 shadow rounded p-5">
    <h1 class="text-5xl text-white font-semibold">Upload new link</h1>
    <form action="/new" method="post" class="flex flex-col justify-center items-center gap-5 w-full">
      <input type="text" name="name" placeholder="Name" class="border-2 border-stone-900 rounded-md px-4 py-2">
      <input type="text" name="url" placeholder="URL" class="border-2 border-stone-900 rounded-md px-4 py-2">
      <input type="submit" value="Upload" class="border-2 border-stone-900 rounded-md px-4 py-2 bg-gray-400">
  </div>
  {# <div class="h-10"></div> #}
</body>
</html>