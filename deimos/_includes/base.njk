<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>{{title}}</title>
  <link rel="stylesheet" href="/styles.css">
  <script src="https://unpkg.com/htmx.org@1.9.5"></script>
</head>
<body class="bg-stone-800 h-screen">
  <div class="flex h-10 justify-end items-center gap-10 mx-9 my-2">
    {% for top in tops %}
      <button hx-post="{{ top.url }}" hx-trigger="click" hx-target="#list-contents" hx-trigger="every 5s" class="text-stone-900 bg-stone-100 shadow hover:shadow-md shadow-white/90 rounded-md px-4 py-2">{{ top.name | safe }}</a>
    {% endfor %}
  </div>
  <div id="list-contents" class="container mx-auto flex justify-center flex-col gap-5 items-center mt-10">
    {{ content | safe }}
  </div>
  {# <div class="h-10"></div> #}
</body>
</html>